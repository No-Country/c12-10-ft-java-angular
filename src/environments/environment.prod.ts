
const host = 'https://';
const hostAuth = 'https://';

export const environment = {
    production: true,
    url: host,
    // apiExternalUrl: hostAuth + 'api/v1',
    // apiUrl: host + 'api/v1',
};
